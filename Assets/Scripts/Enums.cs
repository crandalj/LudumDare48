using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Enums
{
    public enum EventEffectType { NULL, HEALTH, MONEY, FUEL, MORALE }
    public enum PlanetEventType { INVESTIGATE, MINE, RESEARCH, MAP, LAND, TRADE, COMBAT } 
    public enum ShipEventType { REST, DISCIPLINE, BUILD, MAINTENANCE}
}
